// Copy this file to `secrets.h` and fill the values. Do NOT commit `secrets.h`.
// Example for `DHT11_Test` project.

// WiFi (copy these names exactly as used by the firmware)
const char* ssid = "YOUR_SSID";
const char* password = "YOUR_WIFI_PASSWORD";

// Azure IoT Hub (host without scheme)
const char* IOTHUB_HOST = "your-iothub.azure-devices.net";
const char* DEVICE_ID = "yourDeviceId";
const char* DEVICE_KEY = "BASE64_DEVICE_KEY"; // primary key (base64)

// Optional: example SAS token (only for testing, usually generated on device)
// const char* TOKEN_EJEMPLO = "SharedAccessSignature sr=...&sig=...&se=...";
